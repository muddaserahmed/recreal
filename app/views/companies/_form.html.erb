<%= form_for(@company) do |f| %>
  <% if @company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% @company.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!-- Customized code for polymorphic user -->
<div class="large-6 columns">
<%=   fields_for :user do |user_fields| %>
<%=   user_fields.label :email %>
<%=   user_fields.text_field :email %><br />
<%=   user_fields.label :password %>
<%=   user_fields.text_field :password %><br />
<%=   user_fields.label :password_confirmation %>
<%=   user_fields.text_field :password_confirmation %><br />
<%end%>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :website %><br />
    <%= f.text_field :website %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
